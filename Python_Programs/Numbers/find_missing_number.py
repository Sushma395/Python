
def merge(arr, N, n):
    if arr.count('NA') != len(N):
        return 'merge count is not matching'
    j = 0
    for i in range(0,len(arr)) and :
        if arr[i] == 'NA':
            arr[i]

arr = [2, 8, NA, NA, NA, 13, NA, 15, 20]
N = [5, 7, 9, 25]
n = len(N)
